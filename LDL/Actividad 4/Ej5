# Solicitar al usuario que ingrese el día de la semana y la cantidad de artículos
# comprados por un cliente en un comercio. Finalmente, imprimir “accede al
# descuento” si el día es lunes y el cliente compró más de 3 artículos. En caso
# contrario, no imprimir nada.

dias = (input("Ingresar día de la semana: "))
prod = int(input("Ingresar cantidad de productos: "))

if dias.lower() == 'lunes' and prod >= 3:
    print("Accede al descuento.")
else: print("No accede al descuento.")