# Escribir un programa que solicite el ingreso de un número entero y, si el número
# leído es par, imprima la leyenda 'El número es PAR'. En caso contrario, deberá
# mostrar el texto 'El número es IMPAR'.

num = int(input("Ingresar número entero: "))
if (num % 2 == 0):
    print("El número entero ingresado: '{}' es par".format(num))
else: print("El número entero ingresado: '{}' es impar".format(num))
