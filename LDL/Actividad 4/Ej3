# Modificar el programa anterior para que ahora solicite el ingreso de dos números
# enteros, y que luego informe si el primero es o no mayor que el segundo, usando
# el formato 'X es mayor que Y' (o ‘X no es mayor que Y’). Si ambos números fueran
# iguales, deberá informar 'X es igual a Y'. Por ejemplo, si se ingresan 23 y 42, se
# mostraría '42 es mayor que 23'.

x = int(input("Ingresar primer número: "))
y = int(input("Ingresar segundo número : "))

if (x > y):
    print("'X': ({}) es mayor que 'Y': ({})" .format(x, y))
elif (x < y): print("'X': ({}) no es mayor que 'Y': ({})" .format(y, x))
else: print("'X': ({}) es igual que 'Y': ({})" .format(x, y))
