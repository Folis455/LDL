# Modificar el programa anterior para que, si se ingresa un número negativo, no se
# sume, pero continúe con el proceso. Finalmente, mostrar por separado la suma
# de los números positivos pares e impares ingresados.

cant = int(input("Ingresar cantidad de números a sumar: "))
suma = 0

for i in range(cant):
    num = int(input("Ingresar número a sumar: "))
    if (num < 0): continue
    else: suma += num
print ("La suma es:", suma)